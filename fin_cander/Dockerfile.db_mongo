FROM mongo:latest

ENV container_name mongo

ENV restart always

ENV env_file .env

ENV MONGO_INITDB_ROOT_USERNAME=${MONGO_DB_USERNAME}
ENV MONGO_INITDB_ROOT_PASSWORD=${MONGO_DB_PASSWORD}
ENV MONGO_INITDB_DATABASE=${MONGO_DB_NAME}

EXPOSE 27017

VOLUME ["/data/db"]